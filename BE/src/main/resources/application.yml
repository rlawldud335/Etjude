server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/unnamed?useUnicode=true&serverTimezone=Asia/Seoul
    username: testuser
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
#      use-new-id-generator-mappings: false
    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace